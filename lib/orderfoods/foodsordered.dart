import 'package:flutter/material.dart';
class OrderedFoods extends StatelessWidget {
  const OrderedFoods({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
      title: Text('Ordered Foods'),
    ),
    );
  }
}
